public class Game{
    public static void main(String[] args) {
        //construct the window object 
        //anytime to build an object ->use instanciation (new)
        //declartion (date type->class name ) (win-object name or refernece name) (assignment operator)(new instanciation the object)(window is the constructor of teh class we want to make the object from )

        Window win = new Window();

        
        
        
    }
}

